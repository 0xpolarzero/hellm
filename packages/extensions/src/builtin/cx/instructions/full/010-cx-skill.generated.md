# cx — Semantic Code Navigation

Use `cx overview`, `cx symbols`, `cx definition`, and `cx references` to inspect code semantically before reading whole files. Narrow by file, name, and kind before increasing result limits.
