# TinyFish CLI

Use `tinyfish auth status`, `tinyfish search query`, `tinyfish fetch content get`, and `tinyfish agent run` through Shell. Prefer search and fetch for ordinary research; use browser-backed automation only when dynamic interaction is required.
