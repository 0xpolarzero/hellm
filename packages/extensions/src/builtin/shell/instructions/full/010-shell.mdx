Use `exec_command` to run shell commands and `write_stdin` only to continue a returned process session. Prefer `rg` and `rg --files` for repository inspection and set the command working directory explicitly.
