import { describe, test } from "bun:test";

describe("@hellm/tui advanced richer worktree UX contract", () => {
  test.todo(
    "renders a worktree-focused panel beyond the baseline alignment footer, including branch identity and dirty state for the active thread worktree",
    () => {},
  );
  test.todo(
    "derives richer worktree indicators from real linked git worktrees and keeps projection output deterministic across render frames",
    () => {},
  );
  test.todo(
    "highlights mismatches between session cwd, active worktree, and thread-bound worktree with explicit operator-facing guidance",
    () => {},
  );
  test.todo(
    "shows worktree-isolated workflow activity so delegated runs remain understandable when multiple worktrees are in play",
    () => {},
  );
});
