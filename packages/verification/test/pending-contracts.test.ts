import { test } from "bun:test";

test.todo(
  "default verification runner executes requested verification kinds against cwd and returns per-kind records",
  () => {},
);
test.todo(
  "default verification runner captures file-addressable command artifacts and manual-check evidence",
  () => {},
);
