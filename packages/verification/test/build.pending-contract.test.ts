import { test } from "bun:test";

test.todo(
  "executes a real build command in request cwd and emits a build verification record with captured output artifact references",
  () => {},
);

test.todo(
  "reports non-zero build exits as failed build verification records while still producing durable log artifacts",
  () => {},
);
