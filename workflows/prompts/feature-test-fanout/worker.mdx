You own test coverage for exactly one product feature: {props.featureId}.

Repository instructions:

- Read `AGENTS.md` first.
- Read `docs/prd.md`.
- Read `docs/features.ts` and locate the exact feature entry you own.

Mission:

- Build the fullest maintainable test surface for this feature only.
- Prefer real filesystem, temp repos/worktrees, real process boundaries, real JSONL/session flows, and real TUI rendering harnesses where practical.
- Use mocks, fakes, or stubs only for truly external boundaries: networked services, paid APIs, nondeterministic integrations, or other dependencies outside this repo.
- Do not use mocks, fakes, or stubs to make passing tests for product behavior in this repo that is not implemented yet.
- If this feature depends on an unimplemented in-repo boundary, adapter, or execution path, add or refine pending/todo contract coverage instead of fabricating executable coverage.
- Prefer an explicit `todo`/pending contract over a green test that only passes because an internal fake or stub was invented for missing product behavior.

Scope constraints:

- Do not broaden into neighboring features unless a tiny shared seam is required.
- If you add shared test support, keep it minimal and directly justified by this feature.
- Do not rewrite or classify the whole feature inventory; act only on this feature's coverage.
- Do not spawn subagents or delegate internally; this workflow already parallelizes by feature.
- Do not merge your own branch or delete your own worktree; the workflow will reconcile that later.

Branch and worktree:

- Your branch is {props.branch}.
- Your worktree path is {props.worktreePath}.

Required execution behavior:

- Write or deepen tests for this feature.
- Keep executable tests honest: if the real in-repo behavior does not exist yet, leave the feature pending/todo rather than simulating that missing behavior behind a new internal fake or stub.
- Do not create git commits yourself. Leave any repo changes in the feature worktree; the workflow will verify and commit them after your task completes.
- Run the most focused verification you reasonably can for this feature before you finish.
- Return the verification commands that should be re-run by the workflow verifier.
- If no repo changes are needed, leave the worktree clean and report `committed=false`.

Required structured return fields:

- `featureId`
- `status`
- `summary`
- `testFiles`
- `supportFiles`
- `docsFiles`
- `verificationCommands`
- `committed` (normally `false`; the workflow owns commits)
- `commitSha` when `committed=true`
- `branch`
- `worktreePath`
- `pendingReason` when applicable
