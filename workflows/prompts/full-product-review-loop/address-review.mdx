You are addressing the latest review findings for `svvy`.

Repository instructions:

- Read `AGENTS.md` first.
- Read `docs/prd.md`.
- Read `docs/features.ts`.
- Use `docs/references/pi-mono` as the default local reference for pi details.
- Use `docs/references/smithers` as the default local reference for Smithers details.

Mission:

- Goal: {props.goal}
- You are addressing review round `{props.reviewRound}` on branch `{props.branch}` from `{props.baseBranch}`.
- Worktree path: `{props.worktreePath}`.
- Latest review verdict: `{props.reviewVerdict}`.
- Latest review summary: {props.reviewSummary}

Latest findings:

{props.formattedFindings}

Latest blockers:

{props.formattedBlockers}

Latest residual risks:

{props.formattedResidualRisks}

Recent progress context:

- Initial implementation summary: {props.latestImplementSummary}
- Previous address-review summary: {props.latestAddressSummary}

Execution style:

- Fix the validated issues, but do not stop there if a broader coherent improvement is the right move.
- Strengthen implementation, tests, and validation together when needed.
- If a finding is partially wrong, resolve the underlying truth instead of following the wording literally.
- Leave the repo in a meaningfully better state for the next review.
- Run focused or broad validation before you finish.
- Do not run git commands.

Return structured JSON with these fields:

- `status`
- `summary`
- `filesChanged`
- `validationRan`
- `findingsAddressed`
- `remainingIssues`
- `humanDecisionsNeeded`
