{
  "name": "svvy",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:execute-typescript-api": "bun scripts/generate-execute-typescript-api.ts",
    "start": "bun run generate:execute-typescript-api && vite build && electrobun dev",
    "dev": "bun run generate:execute-typescript-api && bun scripts/dev.ts",
    "clean:session-state": "bun scripts/clean-session-state.ts",
    "build": "bun run generate:execute-typescript-api && vite build && electrobun build",
    "run": "electrobun run",
    "workflow:implement-feature": "bun workflows/scripts/run-implement-feature.ts",
    "test": "bun run generate:execute-typescript-api && bun test $(rg --files src -g '*.test.ts' -g '*.spec.ts' -g '*_test.ts' -g '*_spec.ts')",
    "setup:e2e": "electrobun-e2e setup --config ./electrobun-e2e.config.ts",
    "test:e2e": "ELECTROBUN_E2E_SHARDS=1 electrobun-e2e run --config ./electrobun-e2e.config.ts",
    "typecheck": "bun run generate:execute-typescript-api && bun run typecheck:root && bun run typecheck:workflows",
    "typecheck:root": "tsc --noEmit",
    "typecheck:workflows": "bun run --cwd workflows typecheck",
    "lint": "oxlint . --import-plugin --node-plugin --deny-warnings --fix --fix-suggestions",
    "lint:check": "oxlint . --import-plugin --node-plugin --deny-warnings",
    "format": "oxfmt $(rg --files -g '*.ts' -g '*.tsx' -g '*.svelte' -g '*.toml' -g '!docs/references/**')",
    "format:check": "oxfmt --check $(rg --files -g '*.ts' -g '*.tsx' -g '*.svelte' -g '*.toml' -g '!docs/references/**')"
  },
  "dependencies": {
    "@fontsource-variable/familjen-grotesk": "^5.2.8",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@lucide/svelte": "^1.8.0",
    "@mariozechner/pi-agent-core": "^0.64.0",
    "@mariozechner/pi-ai": "^0.64.0",
    "@mariozechner/pi-coding-agent": "^0.64.0",
    "electrobun": "1.16.0",
    "electrobun-browser-tools": "1.0.1",
    "svelte": "^5.55.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@types/bun": "^1.3.11",
    "electrobun-e2e": "1.0.2",
    "oxfmt": "0.44.0",
    "oxlint": "1.59.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  },
  "engines": {
    "bun": ">=1.3.10"
  },
  "packageManager": "bun@1.3.10"
}
